[project]
name = "at1-perceptron-knn"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "Psyj1", email = "seiji.paulo@proton.me" }
]
requires-python = ">=3.14"
dependencies = [
    "fastapi[standard]>=0.141.1",
    "matplotlib>=3.11.2",
    "numpy>=2.5.3",
    "pandas>=3.0.5",
    "scikit-learn>=1.9.1",
]

[project.scripts]
at1-perceptron-knn = "at1_perceptron_knn:main"

[build-system]
requires = ["uv_build>=0.12.14,<0.13.0"]
build-backend = "uv_build"

[dependency-groups]
dev = [
    "notebook>=7.6.2",
]
